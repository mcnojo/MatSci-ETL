#!/bin/bash
# Placeholder. Step 2 of INFRA_PROVISIONING_PLAN.md replaces this with the
# real bootstrap. Listed variables (${repo_url}, ${repo_ref}, ${temporal_address},
# ${temporal_namespace}, ${max_concurrent_cpu}, ${max_concurrent_gpu},
# ${aws_region}, ${artifact_bucket}, ${lifecycle_queue}) form the interface.
set -euo pipefail
echo "user_data unfilled; land Step 2 before terraform apply" >&2
exit 1
